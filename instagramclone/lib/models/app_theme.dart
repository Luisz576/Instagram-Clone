import 'package:flutter/material.dart';

class AppTheme{
  final Color primaryColor, secundaryColor, primaryTextColor, accentColor;
  final String instagramLogoUrl;

  AppTheme({required this.instagramLogoUrl, required this.accentColor, required this.primaryColor, required this.primaryTextColor, required this.secundaryColor});
}