import 'package:flutter/material.dart';

class ControlStoryPanel extends StatelessWidget {
  const ControlStoryPanel({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO:
    return Container();
  }
}